#ifndef CLIENT_REQUEST_H
#define CLIENT_REQUEST_H

/*
* ClientRequest class represents generic client's requests received
* by the HTTPServer object (@see HTTPServer.hpp) in a format used
* by the RequestsDispatcher class (@see RequestsDispatcher.hpp)
*/

class ClientRequest{
    
};

#endif
