#ifndef SERVER_RESPONSE_H
#define SERVER_RESPONSE_H

/*
* ServerResponse class represents generic server module's response
* that should be sent to a client by the HTTPServer object 
* (@see HTTPServer.hpp). This class constitutes interface
* (along with ClientRequest (@see ClientRequest.hpp)) between
* HTTPServer and RequestsDispatcher (@see RequestsDispatcher.hpp) 
*/

class ServerResponse{
    
};

#endif
